info:
  name: Add Operation
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/calculator?operation=add&num1=10&num2=5"
  params:
    - name: operation
      value: add
      type: query
    - name: num1
      value: "10"
      type: query
    - name: num2
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
