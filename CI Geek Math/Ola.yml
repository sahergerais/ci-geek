info:
  name: Ola
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/hello?name=Leo"
  params:
    - name: name
      value: Leo
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
